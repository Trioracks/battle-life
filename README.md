# Battle Life

Браузерный MVP о начинающем MC из Каликфорнии. Игрок создаёт персонажа, живёт в квартире, работает, следит за базовыми потребностями, регистрируется на MAKAREWITCH VI и готовит трек к дедлайну.

## Запуск

Откройте `index.html` через локальный HTTP-сервер, например:

```powershell
Set-Location 'D:\Project\Battle'
python -m http.server 4173
```

Затем перейдите на `http://localhost:4173`.

## Проверка

```powershell
npm test
```

Сохранение кампании находится в `localStorage` браузера. Для нового старта очистите данные сайта в инструментах браузера.
